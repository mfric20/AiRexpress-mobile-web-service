export type IProduct = {
  productID: string;
  variantKey: string;
  version: number;
  name: string;
  description: string;
  priceId: string;
  amount: number;
  inventoryId: string;
  inventoryVersion: number;
  quantity: number;
};
